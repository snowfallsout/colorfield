<!--
Snapshot note for the repo-wide Colorfield-to-InkLumina cleanup.
Scope: rename internal socket contracts and helpers to InkLumina, update the client/server runtime references, switch the session storage key to the public brand, and keep the protected static reference files untouched.
-->

# DEVNOTES 2026-05-05T170104Z

This change removes the last source-level `Colorfield*` socket naming from the active runtime code and replaces it with `InkLumina*` contract and helper names. The shared socket contracts, client socket factory, server socket wiring, display realtime bindings, and the session storage key now use the public brand, and the remaining source-level share-card text in `src/lib/utils/holoCanvas.ts` now reads `inklumina.live`.

Impact notes:

- The active source tree no longer exposes `Colorfield` in its socket type names or helper names.
- The session storage key is now `inklumina_pc_ip`, so existing saved values under the older key are no longer read.
- Protected static reference files were intentionally left untouched, so they still contain historical `colorfield` strings for comparison purposes.
- `npm run check` passed after the rename cleanup.
