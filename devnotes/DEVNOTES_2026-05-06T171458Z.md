/*
 * Snapshot note for the first legacy utils deletion batch.
 * Scope: remove the confirmed-unused legacy helpers that no longer have direct callers in src/ and keep the canonical services/state owners unchanged.
 */

# DEVNOTES 2026-05-06T171458Z

This snapshot records the cleanup step after the legacy helper sweep. The remaining files under `src/lib/utils/` are no longer part of the active source tree, so this pass can remove the old coordinator, draw-mode, emotion, holo canvas, MBTI, camera, and overlay helpers without changing the canonical display/mobile service owners. The goal is to shrink the compatibility surface, not to move behavior again.

## Task 1: Delete confirmed-unused legacy helpers

### Plan
- Remove the old helper files that no longer have any direct importer in `src/**`.
- Keep the canonical display and mobile service modules intact.

### Todo
- Delete `src/lib/utils/coordinator.ts`.
- Delete `src/lib/utils/drawmode.ts`.
- Delete `src/lib/utils/emotion.ts`.
- Delete `src/lib/utils/holoCanvas.ts`.
- Delete `src/lib/utils/mbti.ts`.
- Delete `src/lib/utils/ml-camera.ts`.
- Delete `src/lib/utils/overlay.ts`.

### Impact
- The old legacy utility surface disappears in one controlled batch.
- The active Svelte 5 owners stay in `state/` and `services/`.

## Task 2: Recheck the remaining ownership map

### Plan
- Re-scan `src/lib/utils/` after the deletion batch.
- Keep any remaining work focused on the canonical service/state owners.

### Impact
- The next cleanup step can target only the true leftovers.
- The repo moves closer to a no-legacy-utils state.