# DEVNOTES_2026-04-28T120000Z

/*
  檔案說明：
  - 記錄於 2026-04-28 啟用專案層級變更守則（來自 .github/copilot-instructions.md）。
  - 每次在專案中進行程式碼修改前，AI agent 需：
    1. 讀取並遵循 `.github/copilot-instructions.md` 的規範；
    2. 在 `devnotes/` 新增本次變更記錄（以 `DEVNOTES_{ISO_TIMESTAMP}.md` 命名）；
    3. 先與維護人員溝通並獲得確認（由使用者或維護者在 issue/PR 中確認）。
*/

## 背景
先前對 `src/lib/components/display/CamToggle.svelte` 的小修改（將 `onclick` 改為 Svelte 的 `on:click`）已被還原；根據 `.github/copilot-instructions.md` 要求，任何修改應先通報並記錄。

## 本次動作
- 啟用「變更前自動讀取 `.github` 並新增 DEVNOTES」流程，並建立此筆 DEVNOTES。
- 已更新 agent 的 todo list（添加 `Enable repo .github pre-change checks and create devnotes`，狀態：completed）。

## 未來流程（agent 將遵守）
1. 在做任何檔案修改前，自動讀取 `.github/copilot-instructions.md` 並產出一份簡短變更計畫，向維護者請求確認。
2. 經確認後，建立 `devnotes/DEVNOTES_{ISO_TIMESTAMP}.md` 描述變更目的與實作細節，然後套用修改。
3. 修改完成後，在該 DEVNOTES 中補充驗證步驟與注意事項。

## 下一步建議
- 請指示是否要現在依此流程重新套用對 `CamToggle.svelte` 的修正（包含 PR/issue 註記與驗證步驟）。

---
*記錄者：AI agent*
*時間（ISO）：2026-04-28T12:00:00Z*
