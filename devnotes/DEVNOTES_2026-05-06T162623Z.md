/*
 * Snapshot note for the next utils/services/state cleanup pass.
 * Scope: remove pure compatibility shells, move display-only helpers next to their canonical state owners, and leave the particle pool/sprite chain for a later pass because it is still transitively active.
 */

# DEVNOTES 2026-05-06T162623Z

This snapshot records the state after validating the current `utils/`, `services/`, and `state/` split. The tree still contains pure forwarding shells in `utils/` and top-level `services/*`, while the display-only helper types and smile hashing logic can be moved next to the canonical state owners. The upcoming change removes the dead wrappers, rehomes the display-specific helper types and face-hash helper, and deliberately leaves `utils/pool.ts` and `utils/sprites.ts` for a follow-up because they still have active consumers through the display particle engine.

## Task 1: Remove pure shells

### Plan
- Delete the compatibility-only wrappers that no longer own behavior.
- Keep the canonical display session helper in `services/display/session.shared.ts` and the canonical display state in `state/display.svelte.ts`.

### Todo
- Delete `src/lib/utils/state.ts`.
- Delete `src/lib/utils/session.ts`.
- Delete `src/lib/services/displayRuntime.ts`.
- Delete `src/lib/services/displaySession.ts`.

### Impact
- The public surface becomes smaller without changing the active owners.
- Imports that already point at canonical modules do not need to move.

## Task 2: Rehome display-only helpers

### Plan
- Move the display sample type definitions into the state domain.
- Move the face-hash helper used by smile state into the state domain as well.
- Remove the stale fake module declaration once the real module exists.

### Todo
- Add `src/lib/state/display.types.ts`.
- Add `src/lib/state/faceHash.ts`.
- Update `state/display.svelte.ts` and `state/smile.svelte.ts` imports.
- Remove the `app.d.ts` declaration for `$lib/utils/faceHash`.

### Impact
- `utils/` stops hosting display-only ownership.
- The state folder becomes the clearer home for the helper code it actually uses.

## Task 3: Leave transitive runtime owners for later

### Plan
- Do not touch the particle pool / sprite chain in this pass.
- Revisit `particleEngine.ts`, `pool.ts`, and `sprites.ts` only after their active consumers are moved.

### Todo
- Keep `src/lib/utils/pool.ts` for now.
- Keep `src/lib/utils/sprites.ts` for now.
- Recheck `src/lib/services/display/particleEngine.ts` in the next pass.

### Impact
- The cleanup stays reversible.
- We avoid deleting helpers that are still transitively in use.
