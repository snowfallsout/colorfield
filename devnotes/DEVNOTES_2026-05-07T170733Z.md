# DEVNOTES_2026-05-07T170733Z

## Task 1

### Plan

- Give `src/lib/config/` a server-only owner for filesystem-backed session persistence config.
- Move session storage path and filename conventions out of `sessions.server.ts`.
- Keep this pass limited to integration config, not route constants or UI copy.

### Todo

- Add `src/lib/config/server.ts`.
- Point `src/lib/server/sessions.server.ts` at the new config owner.
- Validate the change with `npm run check`.

### Result

- Added `src/lib/config/server.ts` with resolved server-only session persistence config.
- Replaced hardcoded session storage path, filename prefix, and default session name in `sessions.server.ts` with config-backed values.

### Impact

- `src/lib/config/` now has both browser-safe public env config and server-only integration config.
- Session persistence can move to a different directory through `INKLUMINA_SESSIONS_DIR` without editing source.
- Route handlers under `src/routes/api/sessions/*` keep the same behavior because they already delegate to `sessions.server.ts`.