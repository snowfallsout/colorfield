/*
 * Snapshot note for the display particle-engine consolidation pass.
 * Scope: keep the mobile state rename stable, then move the display particle engine away from the legacy `utils/pool.ts` and `utils/sprites.ts` chain so the canonical display service owns rendering and particle state.
 */

# DEVNOTES 2026-05-06T170821Z

This snapshot records the current state after the mobile state rename and before the next display-runtime cleanup. `state/` now stays aligned with the requested `.svelte.ts` shape, and the remaining major duplication is the display particle-engine compatibility layer that still delegates to the legacy `utils/pool.ts` and `utils/sprites.ts` helpers. The next step is to retarget that engine to the canonical display service modules, which should let the legacy pool/sprite helpers be retired in a controlled follow-up.

## Task 1: Keep mobile state naming stable

### Plan
- Keep the renamed mobile state file as `src/lib/state/mobile.svelte.ts`.
- Update any stale import paths to the new module name.

### Impact
- The mobile Svelte 5 state owner matches the rest of `state/`.
- The rename stays isolated and easy to verify.

## Task 2: Consolidate display particle ownership

### Plan
- Move `ParticleEngine` off the legacy `utils/pool.ts` and `utils/sprites.ts` chain.
- Prefer the canonical display service state and sprite owners already living under `src/lib/services/display/`.
- Keep the existing Canvas component contract stable while changing the engine internals.

### Impact
- The display runtime becomes more service-centered.
- The legacy utility helpers become candidates for retirement once no other callers remain.

## Task 3: Follow-up cleanup

### Plan
- Re-scan for any remaining `utils/*` references after the particle-engine consolidation.
- Remove dead compatibility helpers only after the runtime has moved off them.

### Impact
- The cleanup stays incremental.
- No legacy helper is removed before the last caller is gone.
