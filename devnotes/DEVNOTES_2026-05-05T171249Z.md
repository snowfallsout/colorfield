<!--
Snapshot note for the mobile helper relocation.
Scope: keep the mobile selection/canvas helpers under src/lib/services/mobile,
move the Svelte state owner to src/lib/state, and update the page/component imports.
-->

# DEVNOTES 2026-05-05T171249Z

This change re-homes the mobile helper modules so the folder layout matches their ownership: `mobile.logic.ts` and `mobile.holo.ts` live under `src/lib/services/mobile/`, while the Svelte 5 mobile flow state now lives under `src/lib/state/mobile.state.svelte.ts`. The mobile page and its presentational child components were updated to import from the new locations, and the local mobile devnote was refreshed to reflect the new ownership map.

Impact notes:

- The mobile runtime behavior is unchanged; this is a path and ownership cleanup only.
- The state owner is now aligned with the rest of the shared state modules under `src/lib/state/`.
- `npm run check` passed after the relocation.