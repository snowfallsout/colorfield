<!--
Snapshot note for the internal naming cleanup.
Scope: replace the remaining branded socket/helper identifiers with generic names, remove brand words from non-UI storage and DOM helper identifiers, and keep user-visible plain text unchanged.
-->

# DEVNOTES 2026-05-05T171702Z

This change removes the remaining branded identifiers from the active source tree. The socket client/server contracts now use generic `Socket`, `createSocket`, `ClientToServerEvents`, and `ServerToClientEvents` names, and the session helper now uses a generic storage key plus generic DOM helper identifiers.

Impact notes:

- Socket behavior and event payloads are unchanged; only type and factory names were simplified.
- User-visible plain text keeps the current branding where it is intentionally shown in the UI or generated card copy.
- The mobile helper relocation from the previous round remains unchanged.
